version: '3.9'
services:
  postgres:
    image: docker.io/library/postgres:16
    environment:
      POSTGRES_DB: r6bot
      POSTGRES_USER: r6bot
      POSTGRES_PASSWORD: r6bot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
